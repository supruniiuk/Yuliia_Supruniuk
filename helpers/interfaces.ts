export interface PlaceOrder {
  name: string;
  country: string;
  city: string;
  creditCard: string;
  month: string;
  year: number;
}

export interface LoginForm {
  username: string;
  password: string;
}

